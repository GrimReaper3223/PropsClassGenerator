package com.dsl.classgen.engine;

/**
 * Verifica se a anotacao GeneratedClass existe. Se sim, e porque 
 * existe a classe principal.
 * Se nao, ela deve ser gerada
 * 
 * Se a classe ja existir, deve verificar se existe uma anotacao GeneratedSubClass
 * Se existir, e porque ja existe uma subclasse gerada. entao devemos adicionar outras subclasses a classe principal
 * 
 * @since
 * @version
 * @author Deiv
 */
public class Checker {

}
